// import { getTranslations } from '@/libs/getTranslations'

// export function useTranslations(lang: string) {
//   // thử chạy được json không nều được mới bỏ lang vào lang
//   const t = getTranslations('en')

//   return function translate(key: string): string {
//     return key.split('.').reduce((acc: any, k: string) => acc?.[k], t) || key
//   }
// }
